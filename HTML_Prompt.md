# AI 儿童理科互动 HTML — 启动 Prompt

请按照《AI 儿童理科互动 HTML 设计 Skill》设计并制作一个新的单主题互动 HTML。

主题：{{主题}}
目标年龄：9–12 岁
预计体验时长：6–10 分钟
输出：单文件 HTML（HTML/CSS/JS，无外部依赖优先）

开始编码前先输出并确认以下内容：

1. 这个主题孩子最后要建立的“一句话科学模型”。
2. 3–4 个最值得操作的科学变量。
3. 2–3 个孩子常见的错误猜想。
4. 对应的 8 阶段学习流程：每阶段写清“目标 / 孩子操作 / 观察结果 / AI 如何引导”。
5. 三个迁移挑战，从日常场景逐渐迁移到更陌生场景。
6. 哪些高阶术语应该主动删除，避免概念过载。

确认流程后再生成 HTML。

制作时必须遵守：

- 第一次学习严格按 1→8 顺序，右上角章节点只显示进度、不可跳转。
- 完成全部流程后，孩子点击“回顾 8 个内容”才解锁章节跳转。
- 场景中的可操作变量优先支持直接拖动，并用滑杆作为精确控制；两者双向同步。
- 拖动使用 Pointer Events + Pointer Capture，不允许拖动时重建整个 SVG 导致卡顿。
- 变化在哪里发生，说明箭头和标尺就放在哪里；不要依赖弹窗解释场景变化。
- 每个假设必须有对应验证实验；错误实验结束后可以“重新选择”，正确则继续。
- 新交互第一次出现时使用“指示线 + 简短说明 + 我知道了”的 onboarding；“需要提示”用于重新显示这些指引。
- 控制科学概念负担，只保留帮助形成核心因果模型的术语。
- 所有真实对象的运动约束要体现在交互中，例如绕行对象使用轨道拖动而不是自由二维拖动。
- 完成后播放撒花动画，并提供录音按钮，让孩子自己复述获得的知识和心得。
- 最后执行 QA：科学准确性、拖拽流畅度、假设分支、标注重叠、文字溢出、主按钮显著性、首次导航锁定、完成后回顾解锁。

# AI 儿童理科互动 HTML 设计 Skill

## 1. Skill 目标

用于快速设计和制作 **9–12 岁儿童的单主题 AI 理科互动 HTML**。

目标不是把教材内容网页化，也不是做选择题小游戏，而是让孩子经历：

**看到奇怪现象 → 主动操作 → 形成猜想 → 用实验验证 → 看见不可见过程 → 建立因果模型 → 解释变化 → 迁移到新情景 → 自己复述知识。**

默认输出为一个可独立打开的单文件 HTML（HTML + CSS + JS），优先无外部依赖。

---

# 2. 使用前必须明确的输入

每次开始新主题前，先确定以下 7 项：

1. **主题**：例如“为什么会有影子”“为什么会打雷”“为什么冰会融化”。
2. **目标年龄**：默认 9–12 岁，不要把 3–12 岁混成一套难度。
3. **核心科学模型**：孩子最后真正需要理解的因果链是什么。
4. **可操作变量**：哪些因素值得让孩子直接改变。
5. **不可见过程**：哪些科学过程需要通过动画/透视模式画出来。
6. **常见错误猜想**：孩子最可能误解成什么。
7. **迁移场景**：学完以后，能用同一个模型解释哪些新现象。

如果主题本身太复杂，先做概念减法。目标不是“讲得尽可能多”，而是让孩子形成一个正确、可迁移的核心模型。

---

# 3. 核心设计原则

## 3.1 先现象，后术语

不要一上来写：

> 今天学习光的直线传播。

应该先让孩子看到一个需要解释的现象，再逐步引出概念。

正确顺序：

**现象 → 操作 → 猜想 → 证据 → 因果 → 科学名称。**

---

## 3.2 让孩子操作“原因”，而不只是点击“下一步”

真正的互动必须改变科学系统里的变量。

例如影子主题可操作：

- 光源位置
- 遮挡物位置
- 承影面位置
- 光源大小
- 材料透明度
- 光源数量

拖动过程中结果必须实时变化。

不要做：

> 拖动 → 松手 → 点击提交 → 才播放结果。

应该做：

> 按住拖动 → 场景连续变化 → 孩子直接看到因果。

---

## 3.3 每次只让孩子关注一个主要变化

一个阶段不要同时教三个概念。

例如孩子移动物体时，可以突出：

- 影子高度变了

而不是同时弹出：

- 高度变化
- 面积变化
- 光照角变化
- 距离公式
- 新术语

信息越多不等于学习越深。

---

## 3.4 错误不是“判错”，而是“实验推翻”

避免：

> ❌ 错误，正确答案是 C。

优先：

> “如果这个猜想是真的，我们应该看到什么？”

然后让孩子做对应实验。

实验结果与预测不一致时，再提示：

> “这个猜想解释不了刚才的现象，再选一个更能解释现象的猜想。”

---

## 3.5 科学准确性优先，但不要概念过载

必须避免明显错误模型，但也不要为了“完整”引入孩子当前不需要的术语。

判断一个概念是否保留时问：

1. 它是否帮助理解核心因果？
2. 后续是否会用到？
3. 不介绍它会不会造成明显科学错误？

三个问题都是否定时，删除。

例如“影子边缘为什么模糊”可以让孩子观察“光源越大，边缘越柔”，但不一定要要求 9–12 岁孩子记住“本影/半影”术语。

---

# 4. 标准 8 阶段学习流程

## 阶段 1：奇怪现象 / 情景进入

### 目的
制造认知冲突，让孩子主动问“为什么”。

### 页面行为
- 一个明确故事情景。
- 只出现最关键的 2–4 个对象。
- 第一次关键操作由新手指引指出。
- 不直接讲科学原理。

### 文案结构
> “发生了一件奇怪的事。”
>
> “试试看改变这个东西。”

### 设计要求
首次进入的新手教程可以用：

- 指示线
- 说明框
- “我知道了”

逐个出现，不要一次铺满屏幕。

---

## 阶段 2：自由调查

### 目的
让孩子找出哪些变量会影响现象。

### 交互
允许改变 2–4 个核心变量。

系统可以自动形成简短“观察记录”：

> 灯关掉 → 影子消失
>
> 物体靠近灯 → 影子发生明显变化

### 原则
只描述发生了什么，不马上解释为什么。

---

## 阶段 3：提出假设

### 目的
逼孩子先形成自己的解释，而不是等 AI 给答案。

建议 3–4 个选项：

- 一个明显错误
- 一个生活经验型错误
- 一个很有迷惑性的近似解释
- 一个正确或最完整解释

选择之后不要立刻公布答案。

进入：

> “如果这个猜想是真的，我们应该能通过实验看到什么？”

---

## 阶段 4：实验验证

### 目的
用真实交互检验“孩子刚刚选择的那个假设”。

### 关键规则
**选什么，就验证什么。**

禁止所有选项最后都跑到同一个实验。

例如：

- 选“墙复制物体” → 更换承影面
- 选“物体自己产生黑图” → 关闭光源
- 选“挡住一部分光” → 显示光路验证

### 错误分支
预测与实验不符：

- 清楚解释“哪里不符合”
- 主按钮变成“重新选择”
- 不继续验证其他无关选项

### 正确分支
如果实验支持当前假设：

- 给出简短确认
- 直接进入下一阶段

### CTA 设计
关键实验按钮必须是页面最醒目的主按钮。

不要把关键动作做成普通蓝色小按钮。

---

## 阶段 5：科学透视 / 可视化不可见过程

### 目的
把孩子肉眼看不到的因果过程画出来。

适用于：

- 光线
- 空气运动
- 水循环
- 分子运动
- 力
- 电荷
- 声波
- 磁场的简化表示

### 展示方式
一次出现一步：

1. 原因从哪里产生
2. 它怎样传播/变化
3. 遇到对象发生什么
4. 最终为什么出现观察到的现象

不要一次把整张复杂科学图全铺出来。

---

## 阶段 6：解释关键变量

### 目的
从“为什么发生”进一步进入“为什么会变大、变小、变快、变慢、变亮……”

交互重点是：

**改变一个变量 → 结果实时变化。**

可以增加：

- 尺寸箭头
- 距离标尺
- 简短数值
- 前后变化比较

例如：

> 影子高度：35 → 86

但文字必须直接贴近被观察对象，不能让孩子去右侧读完文字再猜场景里哪里变了。

---

## 阶段 7：进阶现象

### 目的
给 9–12 岁孩子一个“原来还能这样”的提升，但不堆高阶术语。

选择标准：

- 必须仍然依赖同一个核心模型
- 可以通过互动直接观察
- 不需要大量前置数学

如果概念只会增加记忆负担，删除。

---

## 阶段 8：迁移挑战

### 目的
验证孩子是否能把模型用于陌生场景。

默认设计 3 个挑战：

### 挑战 1
非常接近日常场景，只改变一个条件。

### 挑战 2
增加一个变量或一个对象。

### 挑战 3
把模型迁移到更宏观、更有惊喜感的场景。

例如影子：

- 路灯
- 多光源
- 日食/月食

### 标题规范
明确显示：

- `8. 迁移挑战 · 挑战 1/3`
- `8. 迁移挑战 · 挑战 2/3`
- `8. 迁移挑战 · 挑战 3/3`

不要突然用“第三个挑战”等不一致称呼。

---

# 5. 完成后的结尾流程

全部 8 个部分完成后，不要直接回首页。

应该进入“学习完成态”。

## 5.1 庆祝

可以使用：

- 撒花/confetti
- 完成徽章
- 8 个发现全部点亮

庆祝应该短，不要妨碍后续操作。

## 5.2 让孩子自己说出来

提供录音按钮：

> 🎙 开始录音

鼓励孩子回答：

1. “今天你发现了什么？”
2. “你能用自己的话解释为什么吗？”
3. “哪个实验最让你意外？”

如果浏览器支持 MediaRecorder，可以：

- 请求麦克风权限
- 录音
- 停止
- 页面内播放

如果不支持，显示非阻塞提示，不影响完成流程。

## 5.3 回顾模式

第一次学习过程中，右上角 8 个章节圆点：

- 只表示进度
- 不允许跳转

完成全部流程后，孩子必须主动点击：

> **回顾 8 个内容**

此时才：

- 解锁 1–8 快速导航
- 出现一次导航使用提示
- 允许自由回到任意阶段复习

这样第一次学习不会跳过关键推理链。

---

# 6. 页面布局规范

桌面端默认推荐：

```text
┌────────────────────────────┬──────────────┐
│ 阶段标题 / 当前任务         │ AI 小助手     │
│ ● ● ● ○ ○ ○ ○ ○          │              │
├────────────────────────────┤ 说明 / 控件   │
│                            │              │
│      主科学互动场景         │ 假设 / 实验   │
│                            │ 滑杆 / 按钮   │
│                            │              │
├────────────────────────────┴──────────────┤
│ 上一步        需要提示        继续探索 →   │
└───────────────────────────────────────────┘
```

建议比例：

- 左侧实验场景：约 65%
- 右侧 AI / 控制面板：约 35%

主科学现象必须优先占据视觉面积。

---

# 7. 页面元素规范

## 7.1 顶部

包含：

- 当前阶段名称
- 章节进度点
- 当前模式小标签

第一次学习时章节点不可点击。

---

## 7.2 主场景

优先使用 SVG 实现需要拖拽和动态变化的科学场景。

优势：

- 容易控制几何关系
- 可以直接绑定 pointer 事件
- 文字、箭头、对象位置可同步
- 比纯 DOM 更适合光线、轨迹、影子、波等图形

---

## 7.3 右侧 AI 面板

只负责：

- 当前任务
- 问题
- 假设
- 关键控制器
- 实验结果

不要让 AI 面板替代主场景。

孩子应该主要“看实验”，而不是“看 AI 长文”。

---

## 7.4 主按钮

页面同时最多一个最强 CTA。

例如：

- 执行实验
- 继续探索
- 重新选择

主按钮必须比“上一步”“需要提示”等辅助按钮明显。

---

# 8. 拖拽交互工程规范

这是最容易出问题的部分。

## 8.1 必须连续拖动

使用 Pointer Events：

- `pointerdown`
- `pointermove`
- `pointerup`
- `setPointerCapture`

不要依赖鼠标离开对象后仍然能收到事件。

---

## 8.2 拖动过程中不要重建整个 SVG

一个典型错误：

> pointermove → innerHTML 重绘整个场景

这样会销毁当前拖拽对象，导致：

- 卡顿
- 只能拖一点
- pointer capture 丢失

正确方式：

- 保持 SVG DOM 节点稳定
- 只修改对象的 `x/y/transform/path` 等属性
- 必要时用 `requestAnimationFrame` 合并更新

---

## 8.3 直接拖动 + 滑杆作为双入口

适合孩子的设计通常是：

- 场景里可以直接拖
- 右侧有滑杆做精确控制
- 两者双向同步

例如光源大小：

- 左侧直接上下拉伸光源
- 右侧“光源大小”滑杆同步变化

---

## 8.4 拖动结束后的文字反馈必须基于“本次拖动起点”

不要一直和页面最初状态比较。

应该：

1. pointerdown 记录当前值
2. pointerup 获取新值
3. 比较本次变化
4. 输出对应反馈

否则会出现：

> 明明影子明显变大，却提示“大小几乎没变”。

---

# 9. 场景标注与指示线规范

## 9.1 不要用弹窗解释场景变化

“哪里发生了什么”应该直接标在“哪里”。

例如：

- 影子旁边画高度箭头
- 地面影子旁画长度标尺
- 光源旁标“拖这里”

---

## 9.2 同一时刻只显示必要标注

避免：

- 两个说明框重叠
- 箭头交叉
- 线穿过文字
- 文字超出框线

---

## 9.3 说明框必须自动换行

SVG 中不要假设一行文字永远放得下。

需要：

- 限制最大宽度
- 按中文字符宽度拆行
- 动态计算高度

---

## 9.4 指示线优先折线，不要到处用直线穿屏

推荐：

`说明框 → 水平/垂直折线 → 目标箭头`

每条线只指一个目标。

---

# 10. 新手教程与“需要提示”

第一次进入一个新的主要交互机制时，可以采用 App 式 onboarding：

1. 突出一个对象
2. 一条指示线
3. 一句短说明
4. “我知道了”
5. 下一条说明

完成 onboarding 后，不长期覆盖场景。

“需要提示”按钮的作用不是弹答案，而是：

- 重新显示当前阶段的关键指示线
- 重播当前操作说明
- 必要时逐级增加提示

提示顺序：

1. 看哪里
2. 做什么
3. 比较什么
4. 最后才解释答案

---

# 11. 文案规则

## AI 角色不要像判卷老师

不要：

> “错误。”

改为：

> “这个结果和我们的预测一样吗？”

不要：

> “正确答案是……”

改为：

> “这个实验支持了你的猜想。现在把看不见的过程打开看看。”

---

## 单条文字长度

右侧 AI 对话尽量控制在 1–3 个短段落。

场景内说明尽量一句话解决。

不要把教材正文塞进页面。

---

# 12. 科学模型设计方法

每个主题开始前先写一句：

> **孩子最后应该能用什么因果模型解释现象？**

例如影子：

> 找光源 → 判断光怎么传播 → 看哪个物体挡住光 → 找哪里收到的直接光变少 → 预测影子。

其他主题也必须先有这种模型。

例如“为什么会打雷”：

> 云内电荷分离 → 电势差不断增大 → 空气被击穿形成闪电 → 空气瞬间高温膨胀 → 形成冲击波 → 听到雷声。

然后再决定交互，不要先想“做什么动画”。

---

# 13. 新主题快速拆解模板

## A. 奇怪现象
什么现象最容易让孩子产生“为什么”？

## B. 关键对象
场景里最少需要哪些对象？

## C. 可操作变量
孩子真正能改变哪些原因？

## D. 常见错误猜想
至少设计 2 个真实常见误解。

## E. 验证实验
每个假设分别怎样验证？

## F. 科学透视
哪个不可见过程必须画出来？

## G. 关键变量
改变哪个因素能让结果发生有意义的连续变化？

## H. 进阶现象
有什么“有惊喜但不过载”的进一步发现？

## I. 三个迁移挑战
从近到远设计三个新情景。

## J. 完成复述
孩子最后要能用自己的话说出什么？

---

# 14. 常见雷点

## 雷点 1：年龄跨度过大

“3–12 岁通用”通常意味着任何年龄都不好用。

先锁定年龄段。

---

## 雷点 2：看起来能拖，实际上拖不动

原因通常是拖动时不断重建节点。

所有直接操作必须真实测试连续拖动。

---

## 雷点 3：交互只是换 PPT

如果孩子的主要动作是：

> 下一步 → 下一步 → 选择答案

那不是科学互动。

---

## 雷点 4：实验没有针对假设

选 A、B、C、D 最后都做同一个实验，会破坏推理逻辑。

必须做到：

**假设 → 预测 → 对应实验 → 结果 → 修正模型。**

---

## 雷点 5：视觉结果违反真实世界

例如：

- 影子莫名穿进地下
- 光线没有来源
- 月球可以随意飞到任何位置

科学示意可以简化，但不能和孩子日常观察明显冲突。

如果对象本身受运动约束，就让交互也体现约束。

例如月球应沿地球轨道拖动，而不是自由二维拖动。

---

## 雷点 6：解释框和箭头比科学对象更抢眼

标注是辅助，不是主角。

---

## 雷点 7：一次显示太多标注

孩子不知道该看哪里。

一个时刻突出一个主要观察目标。

---

## 雷点 8：概念过载

不是所有正确术语都值得放进去。

删掉不会帮助当前模型的名词。

---

## 雷点 9：第一次学习就允许随意跳关

会破坏“猜想 → 验证 → 建模”的链条。

快速导航只在第一次完整完成后解锁。

---

## 雷点 10：完成后只有“恭喜”

必须让孩子主动复述。

“自己说出来”是验证是否真正建立模型的重要一步。

---

# 15. 开发顺序

不要一开始就写完整 HTML。

推荐顺序：

### Step 1：确定科学核心模型
一句话写清因果链。

### Step 2：列出 8 阶段
每阶段只写：目标、操作、观察、AI 一句话。

### Step 3：画状态机
包括：

- 当前阶段
- 当前假设
- 实验是否完成
- 挑战完成状态
- 首次学习 / 回顾模式

### Step 4：先做最核心交互
先确认拖拽、变量和科学结果实时联动。

### Step 5：再加视觉说明
箭头、标尺、说明框、新手教程。

### Step 6：再加 AI 文案
不要用文案掩盖交互本身的问题。

### Step 7：加迁移挑战
确保不是重复前面操作，而是真的换场景应用模型。

### Step 8：加完成、录音、回顾
形成完整学习闭环。

---

# 16. QA 检查表

交付前必须至少检查一遍以下项目。

## 科学
- [ ] 核心因果是否正确？
- [ ] 是否有为了动画效果而造成的错误现象？
- [ ] 是否出现不必要的高阶术语？

## 流程
- [ ] 8 阶段是否形成连续推理？
- [ ] 选择什么假设就验证什么？
- [ ] 错误后能重新选择？
- [ ] 正确后能自然继续？

## 拖拽
- [ ] 能按住连续拖动？
- [ ] 不会卡顿、跳回？
- [ ] 场景直接拖动与滑杆是否同步？
- [ ] 拖动范围是否符合真实约束？

## 视觉
- [ ] 文字没有超出说明框？
- [ ] 箭头没有明显互相穿插？
- [ ] 标注没有覆盖关键对象？
- [ ] 变化发生在哪里，提示就在哪里？

## 按钮
- [ ] 当前主操作是否最醒目？
- [ ] 不可执行按钮是否有明确状态？
- [ ] “继续探索 / 重新选择”等文案是否与当前状态一致？

## 首次学习
- [ ] 新手教程是否逐步出现？
- [ ] “需要提示”能重新显示指引？
- [ ] 章节导航是否锁定？

## 完成
- [ ] 是否有完成庆祝？
- [ ] 是否提供孩子自己复述的入口？
- [ ] 点击“回顾 8 个内容”之后才解锁章节导航？

---

# 17. 推荐最终交付物

每个主题最终最好输出三个东西：

1. **单文件 HTML**：实际互动产品。
2. **主题设计稿 Markdown**：记录该主题的科学模型与 8 阶段内容。
3. **QA 结果**：列出测试过的拖拽、分支、导航与科学模型。

这样以后修改不是直接在代码里猜，而是先看设计逻辑。

---

# 18. 本 Skill 的一句话原则

> **不要急着把答案告诉孩子。先让孩子改变世界、看到证据、推翻错误猜想，再让科学解释成为他刚刚经历过的事情的名字。**
